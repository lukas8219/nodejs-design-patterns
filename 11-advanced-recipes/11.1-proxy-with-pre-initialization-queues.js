/* 
using java script proxy object, create a wrapper for adding pre initialization queues to any object.

You should allow the consumer of the wrapper to decide which methods to augment and the name of the property/event that indicates if the component is initialized or not.
*/